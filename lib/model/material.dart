import 'package:flutter/material.dart';

class MaterialModel{
  String? title;
  String? materialType;
  Color? colors;
  MaterialModel({required this.title, required this.materialType, required this.colors});
}